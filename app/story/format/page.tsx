import AppTabs from "@/components/AppTabs";

export const metadata = {
  title: "Format of the Story — Winifred Coss Family Tree",
  description: "How names, dates, and conventions are written in the family story.",
};

export default function StoryFormatPage() {
  return <AppTabs initialTab="story" />;
}
